/*
 * Objetivo del archivo:
 * Centralizar la URL base del backend generado previamente.
 *
 * Que debe completar el estudiante:
 * Si cambia el puerto del backend, debe actualizar API_BASE_URL.
 *
 * Resultado esperado:
 * Los servicios HTTP no deben repetir strings como http://localhost:3000/api.
 */
export const API_BASE_URL = 'http://localhost:3000/api';
